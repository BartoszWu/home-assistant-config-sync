name: "HA Config Sync — Import"
description: "Review GitHub changes and apply approved dashboards to Home Assistant"
version: "0.4.4"
slug: "ha_config_sync_import"
url: "https://github.com/BartoszWu/home-assistant-config-sync"

arch:
  - amd64

startup: application
boot: auto
init: false
stage: experimental

ingress: true
homeassistant_api: true
ingress_port: 8099
panel_title: "HA Config Sync — Import"
panel_icon: "mdi:file-compare"
panel_admin: true

map:
  - type: addon_config
    read_only: true
    path: /review

schema: false
